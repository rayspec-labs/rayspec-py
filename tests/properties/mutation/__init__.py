# SPDX-License-Identifier: Apache-2.0
"""A mutation-testing harness for the modules a mistake in would be expensive.

See :mod:`tests.properties.mutation.mutate` for the tool and the module docstring for how to
run it. Nothing here is collected by pytest except
``tests/properties/test_mutation_operators.py``, which checks the harness itself.
"""
