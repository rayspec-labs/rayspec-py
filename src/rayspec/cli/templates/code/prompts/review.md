Review these files under `{{ inputs.target }}`:

{{ steps.files.output }}

Read them first. List the most important findings (at most five, most important first), then
decide: `approve` when nothing blocks, `request_changes` otherwise, with a one-paragraph summary.
